244:                  onClick={() => setMode('select')}
248:                  )}
254:                  onClick={() => setMode('join')}
258:                  )}
264:                  onClick={() => setMode('create')}
268:                  )}
279:                    {user.photoURL ? <img src={user.photoURL} alt="User" /> : user.email?.charAt(0).toUpperCase()}
290:                onClick={() => logout()}
334:                )}
349:                  )}
351:                  <div className={cn("p-2 rounded-xl", errorMsg ? "bg-red-500/20" : "bg-green-500/20")}>
356:              )}
373:                        onClick={() => onSelect(c)}
380:                               {c.name.charAt(0).toUpperCase()}
400:                                  <span className="block text-sm font-black text-white">{(c.memberEmails?.length || 0) + (c.employees?.length || 0)}</span>
438:                          <button onClick={() => setMode('create')} className="flex-1 px-8 py-5 bg-blue-600 text-white rounded-2xl font-black text-[11px] uppercase tracking-widest hover:bg-white hover:text-blue-600 transition-all shadow-xl shadow-blue-600/20 active:scale-95">Déployer Espace</button>
439:                          <button onClick={() => setMode('join')} className="flex-1 px-8 py-5 bg-white/5 text-white border border-white/10 rounded-2xl font-black text-[11px] uppercase tracking-widest hover:bg-white/10 transition-all active:scale-95">Rejoindre avec Clé</button>
449:                                <div className={cn("px-2 py-0.5 rounded-md text-[8px] font-black uppercase tracking-widest", connStatus === 'ok' ? "bg-emerald-500/10 text-emerald-500 border border-emerald-500/20" : "bg-amber-500/10 text-amber-500 border border-amber-500/20")}>
457:                                      <span className="block text-[9px] font-black text-slate-300 truncate">{user.uid.slice(0, 8)}...</span>
465:                                  onClick={() => window.location.reload()}
476:                  )}
477:           )}
479:              )}
500:                          onChange={e => setNewCompanyName(e.target.value)}
509:                        <button type="submit" disabled={submitting || !newCompanyName.trim()} className="w-full py-6 bg-blue-600 text-white rounded-3xl font-black text-xs uppercase tracking-widest hover:bg-blue-500 transition-all shadow-2xl shadow-blue-600/20 active:scale-95">
514:                          onClick={() => setMode('select')} 
523:              )}
544:                          onChange={e => setJoinCodeInput(e.target.value.toUpperCase())}
555:                        <button type="submit" disabled={submitting || !joinCodeInput.trim()} className="w-full py-6 bg-indigo-600 text-white rounded-3xl font-black text-xs uppercase tracking-widest hover:bg-indigo-500 transition-all shadow-2xl shadow-indigo-600/20 active:scale-95">
560:                          onClick={() => setMode('select')} 
569:              )}
576:                     <div className={cn("w-2 h-2 rounded-full", connStatus === 'ok' ? "bg-green-500 shadow-[0_0_10px_#22c55e]" : "bg-amber-500 animate-pulse")} />
588:                  <button onClick={() => logout()} className="px-5 py-2.5 bg-white/5 hover:bg-red-500/10 text-[10px] font-black text-slate-400 hover:text-red-400 uppercase tracking-widest rounded-xl border border-white/5 transition-all">Déconnexion</button>
638:            )}
644:            )}
654:          )}
677:            )}
1156:                  onClick={() => window.location.reload()}
1162:            )}
1185:             <div className="flex items-center gap-3 cursor-pointer" onClick={() => setShowMarketplace(false)}>
1190:               onClick={() => setShowMarketplace(false)}
1197:        <Marketplace onBack={() => setShowMarketplace(false)} />
1203:    return <LoginScreen onMarketplace={() => setShowMarketplace(true)} />;
1232:            onClick={() => auth.signOut()}
1271:        onClose={() => setIsCommandPaletteOpen(false)}
1272:        onNavigate={(tab) => setActiveTab(tab)}
1279:          onClick={() => setSidebarOpen(false)}
1282:      )}
1288:          onClick={() => setSidebarOpen(false)}
1291:      )}
1304:        )}
1341:                        ))}
1350:                )}
1352:            )}
1356:            onClick={() => setSidebarOpen(!isSidebarOpen)} 
1377:                )}
1382:                )}>
1389:                  )}>{item.label}</span>
1390:                )}
1396:                )}
1401:            ))}
1411:                onClick={() => setActiveTab('ecommerce')}
1417:          )}
1419:            onClick={() => setCurrentCompany(null)}
1446:              onClick={() => setSidebarOpen(!isSidebarOpen)} 
1461:              onClick={() => setIsCommandPaletteOpen(true)}
1488:                      {user.displayName?.charAt(0)?.toUpperCase()}
1490:                  )}
1505:            {activeTab === 'marketplace' && <Marketplace onBack={() => setActiveTab('dashboard')} />}
1522:          onClose={() => setIsHelpOpen(false)} 
1540:              )}>
1547:          )}
1577:               onClick={() => setActiveTab(item.id)}
1581:               )}
1590:               )}
1592:           ))}
